from setuptools import setup, find_packages

setup(
    name='decipher-backend',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # List your project's dependencies here.
        # For example: 'requests>=2.24.0',
    ],
    # Additional metadata about your project
)
